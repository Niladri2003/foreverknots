# Copy to `.env` (or `.env.local`) and fill in. Vite exposes VITE_* to the app.

# Google Apps Script Web App /exec URL that receives contact-form enquiries
# and sends the auto thank-you. See apps-script/SETUP.md to create it.
VITE_CONTACT_ENDPOINT=
